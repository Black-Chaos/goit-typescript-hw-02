// Перетворіть цей код на TypeScript, вказавши відповідні типи для всіх змінних.

let age = 50;
let name = 'Max';
let toggle = true;
let empty = null;
// Для всіх попередніх вказано значення при оголошенні і значить їм вже присвоєні типи.

let notInitialize: any;
let callback = (a:number):number => { return 100 + a };

export {};